[project]
name = "accelerate-web3"
authors = ["Web3 Developer Team"]
description = "A decentralized asset management and tracking platform"
telemetry = false
requirements = []
[contracts.web3-vault-core]
path = "contracts/web3-vault-core.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
